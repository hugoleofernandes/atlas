info:
  name: Lookups-GetStatuses-En
  type: http
  seq: 4

http:
  method: get
  url: "{{bff_api_prefix}}/bff/v1/platform/lookups/statuses"
  headers:
    - name: Accept-Language
      value: en
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
