info:
  name: Health-Ready
  type: http
  seq: 2

http:
  method: GET
  url: "{{bff_api_prefix}}/health/ready"
  headers:
    - name: Accept-Language
      value: pt
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
