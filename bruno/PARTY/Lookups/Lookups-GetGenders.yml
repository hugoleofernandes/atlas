info:
  name: Lookups-GetGenders
  type: http
  seq: 3

http:
  method: get
  url: "{{bff_api_prefix}}/bff/v1/party/lookups/genders"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
