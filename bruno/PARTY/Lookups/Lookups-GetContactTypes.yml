info:
  name: Lookups-GetContactTypes
  type: http
  seq: 2

http:
  method: get
  url: "{{bff_api_prefix}}/bff/v1/party/lookups/contact-types"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
