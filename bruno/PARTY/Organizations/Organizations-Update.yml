info:
  name: Organizations-Update
  type: http
  seq: 2

http:
  method: put
  url: "{{bff_api_prefix}}/bff/v1/party/organizations/%7Bid%7D"
  headers:
    - name: Content-Type
      value: application/json
    - name: X-XSRF-TOKEN
      value: "{{xsrf_token}}"
  body:
    type: json
    data: |-
      {
        "id": "{id}",
        "legalName": "Acme Comércio Ltda",
        "tradeName": "Acme",
        "legalType": "Ltda"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
