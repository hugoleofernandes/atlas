info:
  name: "Organizations-Deactivate "
  type: http
  seq: 3

http:
  method: patch
  url: "{{bff_api_prefix}}/bff/v1/party/organizations/%7Bid%7D/deactivate?X-XSRF-TOKEN={{xsrf_token}}"
  params:
    - name: X-XSRF-TOKEN
      value: "{{xsrf_token}}"
      type: query
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
