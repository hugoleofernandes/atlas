info:
  name: Persons-List
  type: http
  seq: 6

http:
  method: get
  url: "{{bff_api_prefix}}/bff/v1/party/persons?isActive=true&classification=Customer"
  params:
    - name: isActive
      value: "true"
      type: query
    - name: classification
      value: Customer
      type: query
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Filtros opcionais. classification aceita Staff ou Customer.
