info:
  name: Permissions-List
  type: http
  seq: 1

http:
  method: GET
  url: "{{bff_api_prefix}}/bff/v1/identity/permissions?isActive=true"
  params:
    - name: isActive
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
