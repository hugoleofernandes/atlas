info:
  name: Roles-Lookup
  type: http
  seq: 2

http:
  method: GET
  url: "{{bff_api_prefix}}/bff/identity/roles/Lookup"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
