info:
  name: Roles-List
  type: http
  seq: 1

http:
  method: GET
  url: "{{bff_api_prefix}}/bff/identity/roles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
