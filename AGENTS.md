# Codex Instructions

All repository conventions and architectural rules are maintained in:

```
source-code/CLAUDE.md
source-code/.claude/docs/
```

Read `CLAUDE.md` first — it indexes every doc file. The docs cover architecture, DDD, handlers, endpoints, queries, seeding, permissions, DI, and more.
